# Legacy InfiniSplat references

These P20/P23/P24 files are retained for historical provenance only. They are not claimed to be the exact P32 producer for this scene. The executable P32-style wrapper is the sibling `run_topdown_to_condition.sh` and its Python modules, which use the official InfiniSplat runtime plus the audited P32 camera payload and lambda.
